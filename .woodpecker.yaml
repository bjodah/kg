variables:
  - &ci_image 'cont-reg.bjodah.se:443/bjodah/triceratops-4:41'

when:
  - event: [push]

steps:
  - name: build-and-test
    image: *ci_image
    environment:
    commands:
      - bash -l .ci/run-ci-steps.sh
