variables:
  - &ci_image 'cont-reg.bjodah.se:443/bjodah/triceratops-5:44'

when:
  - event: [push]

steps:
  - name: build-and-test
    image: *ci_image
    environment:
      # Hosted CI runs everything, including the expensive steps a local
      # run-ci-steps.sh skips (the suite under valgrind).
      CI_EXPENSIVE: "1"
    commands:
      - bash -lc "rustup component add rust-analyzer"
      - bash -l .ci/run-ci-steps.sh
