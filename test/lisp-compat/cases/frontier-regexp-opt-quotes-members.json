{
 "id": "frontier-regexp-opt-quotes-members",
 "setup": [],
 "expr": "(let ((re (regexp-opt (list \"a.c\" \"a+b\")))) (list (string-match re \"axc\") (string-match re \"a.c\") (match-end 0) (string-match re \"a+b\")))",
 "note": "MEMBERS ARE LITERAL TEXT, and the regexp has to say so: Emacs answers (nil 0 3 0). \"a.c\" matches the three characters `a', `.', `c' and NOT \"axc\", and \"a+b\" is not a repetition. `regexp-opt' regexp-quotes what it is given, which kg can do with the `regexp-quote' it already has and agrees with Emacs on. This is the property `s-replace-all' depends on for a replacement key containing a period -- frontier-s-el-replace-all-route's second element is that call at package level. kg answers `void-function' at this pin. THIS CASE FLIPPED in the frontier demand phase: kg `regexp-quote's each member with the native it already had.",
 "expect": "agree"
}
