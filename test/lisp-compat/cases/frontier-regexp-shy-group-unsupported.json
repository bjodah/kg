{
 "id": "frontier-regexp-shy-group-unsupported",
 "setup": [],
 "expr": "(list (string-match \"\\\\(?:a\\\\)\" \"a\") (string-match \"\\\\(?:a\\\\)\" \"?:a\") (string-match \"\\\\(?:ab\\\\|a\\\\)\" \"ab\") (match-end 0))",
 "note": "THE CONSTRAINT ON THE IMPLEMENTATION at F.0, and the row the frontier phase did NOT close; the R2 repair tranche closed it in the engine, so kg and Emacs both answer (0 2 0 2) and the case is now an AGREEMENT.  The id keeps its F.0 spelling because a frozen case's id is its name in the manifest and in three plan documents, not a claim about today.  What it measured then: kg's engine had NO shy groups -- it read `\\\\(?:' as a group whose first characters are an ordinary `?' and an ordinary `:', so element two matched the literal text \"?:a\" at 0 where Emacs matches the `a' at 2, and the alternation in element three came out INVERTED, the branch carrying the `?:' being unmatchable.  A MISREADING, not a rejection -- the shape 26.0 found for `\\\\`' and `\\\\'' before 26.2 taught them.  What closed it: tiny-regex-c `29fc9fb` (through fe `e1d4fbd`) compiles `\\\\(?:' to a group that takes no capture register, and reserves every other `\\\\(?' spelling as an invalid regexp rather than the literal characters -- which is what stops `\\\\(?:a\\\\)' matching \"?:a\".  THE CONSEQUENCE THAT WAS THE POINT OF THE CASE is inverted with it: `regexp-opt' MAY now emit `\\\\(?:...\\\\)', the wrapper that makes its result one composable atom, which is what adversarial-review Finding 3 asked for."
}
