{
 "id": "native-load",
 "setup": [],
 "expr": "(load \"path\")",
 "note": "kg's loader resolves against kg's own load-path array and buffer/package state, not a filesystem-agnostic Lisp-only operation: a bare name with no '/' resolves to <config>/kg/lisp/path.el."
}
