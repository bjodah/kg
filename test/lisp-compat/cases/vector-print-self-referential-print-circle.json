{
 "id": "vector-print-self-referential-print-circle",
 "setup": [],
 "expr": "(let ((v (make-vector 2 0))) (aset v 1 v) (let ((print-circle t)) (format \"%S\" v)))",
 "note": "The same object with `print-circle' bound to t: Emacs emits the re-readable labelled form \"#1=[0 #1#]\" rather than the lossy `#0' back-reference its default produces. Recorded beside the default so 24.1's policy choice is made against BOTH of Emacs' behaviours; kg has no `print-circle' variable at this pin, which is itself part of what the decision costs."
}
