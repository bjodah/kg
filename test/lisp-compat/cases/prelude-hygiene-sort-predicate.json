{
 "id": "prelude-hygiene-sort-predicate",
 "setup": [
  "(defvar out 0)"
 ],
 "expr": "(sort (list 3 1 2) (lambda (a b) (setq out a) (< a b)))",
 "note": "kg's `sort' is a Lisp merge sort, so a predicate is user code running inside `internal--merge's accumulator.  With `out' `defvar'd the predicate replaced the merged run with an integer and the sort raised (wrong-type-argument listp 3).  Emacs' `sort' is C; the answer is the sorted list on both sides."
}
