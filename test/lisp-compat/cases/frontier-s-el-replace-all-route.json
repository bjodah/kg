{
 "id": "frontier-s-el-replace-all-route",
 "setup": [
  "(setq s-el-path (if (fboundp 'expand-file-name) (expand-file-name \"external/elpa/s.el\") \"external/elpa/s.el\"))",
  "(condition-case e (load s-el-path) (error nil))"
 ],
 "expr": "(condition-case e (list (s-replace-all (list (cons \"a\" \"X\") (cons \"ab\" \"Y\")) \"abc\") (s-replace-all (list (cons \".\" \"D\")) \"a.b\")) (void-function (car (cdr e))))",
 "note": "THE PACKAGE'S OWN CALL, and it is the longest-match requirement stated where a user can see it. Emacs answers (\"Yc\" \"aDb\"): with both \"a\" and \"ab\" to replace, s-replace-all replaces the LONGER one, so \"abc\" becomes \"Yc\" and not \"Xbc\" -- an implementation of `regexp-opt' that emitted its members in the order given would silently produce the other answer, and nothing in s.el would report an error. The second element is `regexp-quote' at package level: a replacement key of \".\" changes exactly one character. kg answers the missing NAME, `regexp-opt'. THIS CASE FLIPPED in the frontier demand phase: with both \"a\" and \"ab\" to replace, the package replaces the LONGER, which is the whole reason the ordering is part of the contract.",
 "expect": "agree"
}
