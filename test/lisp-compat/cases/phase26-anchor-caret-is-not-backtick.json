{
 "id": "phase26-anchor-caret-is-not-backtick",
 "setup": [],
 "expr": "(list (string-match \"^b\" \"a\\nb\") (string-match \"\\\\`b\" \"a\\nb\") (string-match \"b$\" \"b\\na\") (string-match \"b\\\\'\" \"b\\na\"))",
 "note": "WHERE THE EQUIVALENCE STOPS, and the row that 26.2 does NOT close. Add one newline and Emacs' two spellings part company: (2 nil 0 nil) -- `^' matches after a newline and `$' before one, while `\\\\`' and `\\\\'' are the whole subject's ends and match neither. kg answers (nil nil nil nil), so it is wrong about the first and third for a reason that has nothing to do with the spellings: kg's `^'/`$' anchor the whole subject, a recorded engine divergence (doc/lisp-api.md, and phase15-string-match-anchors-and-case is its own row). After 26.2 this case STILL DIVERGES, on exactly those two elements -- which is the honest reading of `kg's `^' already means what `\\\\`' means': kg's `\\\\`' is RIGHT now and kg's `^' is still WRONG, and they are the same two nodes in fe/tiny-regex-c."
}
