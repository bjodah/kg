{
 "id": "frontier-search-noerror-nil-raises",
 "setup": [],
 "expr": "(with-temp-buffer (insert \"abc\") (goto-char (point-min)) (list (condition-case e (search-forward \"z\") (error (cons (car e) (cdr e)))) (condition-case e (re-search-forward \"z\") (error (car e))) (condition-case e (search-backward \"z\") (error (car e))) (condition-case e (re-search-backward \"z\") (error (car e))) (point)))",
 "note": "NOERROR NIL RAISES, for all four names, and kg's oldest divergence in this family is that it does not. Emacs answers ((search-failed \"z\") search-failed search-failed search-failed 1): one condition, `search-failed', carrying the PATTERN as its data, from every one of the four names, and POINT DOES NOT MOVE. kg answers (nil nil nil nil 1) -- it returns nil on failure whatever it is asked, which is Emacs' NOERROR-t behaviour applied unconditionally, so today kg is silently the wrong one of the three answers. All four names are here because the demand names only `re-search-forward': 26.2's FIXEDCASE precedent is that one argument means one thing across a family, and a NOERROR that raised from three names and not the fourth would be the divergence that costs the most to find. THIS CASE FLIPPED in the frontier demand phase: kg raises `search-failed' from all four names now, carrying the pattern, and point does not move.",
 "expect": "agree"
}
