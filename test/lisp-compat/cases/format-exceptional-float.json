{
 "id": "format-exceptional-float",
 "setup": [],
 "expr": "(format \"%d\" (/ 1.0 0))",
 "note": "05E: kg still refuses %d of the nonfinite float (/ 1.0 0) -- no bignum to print an infinity or NaN into -- but only for the float reason now. (/ 1 0) no longer shares this path: it is integer division and raises arith-error before format sees it."
}
