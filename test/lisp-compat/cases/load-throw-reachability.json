{
 "id": "load-throw-reachability",
 "setup": [
  "(setq load-throw-path (if (fboundp 'expand-file-name) (expand-file-name \"test/lisp-compat/fixtures/load-throw.el\") \"test/lisp-compat/fixtures/load-throw.el\"))"
 ],
 "expr": "(condition-case e (catch 'load-tag (load load-throw-path)) (error (car e)))",
 "expect": "agree",
 "note": "A `throw' out of a loaded file to a `catch' around the (load ...): both dialects answer 99. Emacs evaluates the loaded file inside the dynamic extent of the call; since Phase 12's fix cycle so does kg -- `load' is a prelude read-eval loop on fe's input-unit trio, so no barrier stands between the throw and the catch. This case carried expect: diverge from Phase 11 (kg answered (no-catch load-tag 99), the containment barrier being a throw wall) until the flip; the condition-case is kept from that shape so a regression answers no-catch instead of aborting. The flip and the manifest edit share a commit per the XPASS rule."
}
