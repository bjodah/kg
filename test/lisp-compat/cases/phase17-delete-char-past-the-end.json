{
 "id": "phase17-delete-char-past-the-end",
 "setup": [],
 "expr": "(list\n (with-temp-buffer (insert \"ab\") (goto-char 1) (list (condition-case e (delete-char 9) (error (car e))) (buffer-substring (point-min) (point-max))))\n (with-temp-buffer (insert \"ab\") (goto-char 3) (list (condition-case e (delete-char -9) (error (car e))) (buffer-substring (point-min) (point-max)))))",
 "note": "WAS THE DIVERGENCE, and agrees since Phase 20. Both signal `end-of-buffer'/`beginning-of-buffer' and delete NOTHING when the buffer is too short. kg clamped and deleted what was there until the Phase 20 fe pin added the two names to fe's condition table; the second element of each pair is what the buffer holds afterwards, so the case pinned the data loss while it existed and pins its absence now.",
 "expect": "agree"
}
