{
 "id": "phase18-let-over-local-left-in-other-buffer",
 "setup": [],
 "expr": "(progn (defvar p18v 'D) (with-current-buffer (get-buffer-create \"p18-v1\") (setq-local p18v 'A)) (with-current-buffer (get-buffer-create \"p18-v2\") (setq-local p18v 'B)) (list (with-current-buffer (get-buffer \"p18-v1\")        (let ((p18v 'LET)) (set-buffer (get-buffer \"p18-v2\")) p18v))  (with-current-buffer (get-buffer \"p18-v1\") p18v)  (with-current-buffer (get-buffer \"p18-v2\") p18v)  (default-value 'p18v)))",
 "expect": "agree",
 "note": "Phase 18's ONE predicted divergence, frozen before the code was written and closed by Phase 18's follow-up: a let over a buffer-local binding that is LEFT with a different current buffer than it was entered in. Emacs' specpdl remembers which buffer the binding was made for, and kg's `let' now carries the same thing as an fe binding tag (FE_API_VERSION 11), so the restore names the buffer it displaced rather than whichever one is current at exit. See the phase18-let-buffer-switched-out manifest row."
}
