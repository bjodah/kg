{
 "id": "phase17-looking-at-across-a-line-break",
 "setup": [],
 "expr": "(with-temp-buffer (insert \"ab\\ncd\") (goto-char 1) (looking-at \"ab\\ncd\"))",
 "note": "THE DIVERGENCE. kg's regex engine is handed one row at a time -- the architecture C-s and re-search-forward already have -- so no pattern here matches across a line break. `^' and `$' anchor that row, which is exactly beginning- and end-of-line and is the one place the row-at-a-time model happens to agree with Emacs (phase17-looking-at pins that).",
 "expect": "diverge"
}
