{
 "id": "frontier-s-el-split-up-to-route",
 "setup": [
  "(setq s-el-path (if (fboundp 'expand-file-name) (expand-file-name \"external/elpa/s.el\") \"external/elpa/s.el\"))",
  "(condition-case e (load s-el-path) (error nil))"
 ],
 "expr": "(condition-case e (s-split-up-to \",\" \"a,b,c,d\" 2) (void-function (car (cdr e))) (wrong-number-of-arguments (cdr e)))",
 "note": "THE PACKAGE'S OWN CALL, and the one frontier the probe records as an ARITY rather than a name: Emacs answers (\"a\" \"b\" \"c,d\"), splitting twice and leaving the remainder whole. kg answers (re-search-forward 3) -- the `wrong-number-of-arguments' data, naming the function and the argument count it was given -- which is exactly what test_s_el_vendored_load asserts as the last element of its NEXT list. It is the capability witness for this row: when it answers (\"a\" \"b\" \"c,d\") unmodified s.el got what it asked for."
}
