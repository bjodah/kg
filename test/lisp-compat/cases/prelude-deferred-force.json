{
 "id": "prelude-deferred-force",
 "setup": [],
 "expr": "(list (mapcar '1+ '(1 2)) (mapcar '1+ '(3 4)))",
 "note": "Phase 1 of doc/plans/2026-08-14-embedded-prelude.md. The first `mapcar' call forces the real definition out of the deferred array (internal--force-deferred) and replaces the function cell with it; the second reaches that closure directly, the stub being reachable from nothing by then. Answers ((2 3) (4 5)): forcing happens once per deferred name per session and is invisible in the value either time. No Emacs equivalent -- kg's deferred forms live in a second embedded array, never on disk, so the no-lisp/-directory guarantee is unchanged."
}
