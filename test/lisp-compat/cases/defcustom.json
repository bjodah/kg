{
 "id": "defcustom",
 "setup": [],
 "expr": "(defcustom my-var 5 \"docstring\" :type 'integer)",
 "note": "08E: kg's prelude implements defcustom as a defvar-shaped declaration. It returns the symbol, evaluates the standard-value form only when unbound, stores the docstring, and accepts presentation metadata (:type, :options, :group, :tag, :link, :version, :package-version) without Custom state or a UI."
}
