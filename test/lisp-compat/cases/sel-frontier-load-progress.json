{
 "id": "sel-frontier-load-progress",
 "setup": [
  "(setq sel-frontier-path (if (fboundp 'expand-file-name) (expand-file-name \"test/lisp-compat/fixtures/sel-frontier.el\") \"test/lisp-compat/fixtures/sel-frontier.el\"))"
 ],
 "expr": "(progn (condition-case e (load sel-frontier-path) (error nil)) (list (boundp 'sel-frontier-past-autoload) (boundp 'sel-frontier-past-page-break) (boundp 'sel-frontier-loaded) (fboundp 'sel-frontier-when-let)))",
 "note": "The same load measured by HOW FAR IT GOT, which is what made the sibling case a frontier rather than just a failure and what makes it evidence now: both sides answer (t t t t), where kg answered (t t nil nil) while the vector literal in the `declare' debug spec stopped it -- neither the macro defined nor the trailing form run. A regression that moves the stop back changes this list without changing the sibling's t."
}
