{
 "id": "frontier-s-el-format-aget-route",
 "setup": [
  "(setq s-el-path (if (fboundp 'expand-file-name) (expand-file-name \"external/elpa/s.el\") \"external/elpa/s.el\"))",
  "(condition-case e (load s-el-path) (error nil))"
 ],
 "expr": "(condition-case e (list (s-format \"${a}-${b}\" (quote aget) (list (cons \"a\" \"1\") (cons \"b\" \"2\"))) (s--aget (list (cons \"k\" \"v\")) \"k\")) (void-function (car (cdr e))))",
 "note": "THE PACKAGE'S OWN CALL: `s--aget' is a one-line wrapper over `assoc-string' and `s-format' with the `aget' replacer is its only caller inside s.el, so the two are one demand. Emacs answers (\"1-2\" \"v\"). The first element is the shape a user writes -- a template with ${...} references resolved from an alist -- and it exercises `replace-regexp-in-string' with a FUNCTION replacement, match-data save/restore and `assoc-string' together, which is why it is the row's package witness rather than a second `s--aget' call. kg answers the missing NAME, `assoc-string'."
}
