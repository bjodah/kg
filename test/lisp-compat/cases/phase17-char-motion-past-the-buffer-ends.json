{
 "id": "phase17-char-motion-past-the-buffer-ends",
 "setup": [],
 "expr": "(list\n (with-temp-buffer (insert \"abc\") (goto-char 1) (list (condition-case e (forward-char 20) (error (car e))) (point)))\n (with-temp-buffer (insert \"abc\") (goto-char 3) (list (condition-case e (backward-char 20) (error (car e))) (point))))",
 "note": "WAS THE DIVERGENCE, and agrees since Phase 20. Both signal `end-of-buffer'/`beginning-of-buffer' after moving as far as they can. It diverged because fe gates `signal' on its own condition table (condition_parents[]) and held neither name; the Phase 20 fe pin put both in it. Point ends in the same place on both sides, which is what the second element of each pair pins and which was true while the case still diverged. kg's own `forward-word' and `forward-line' still clamp, and so do Emacs'.",
 "expect": "agree"
}
