{
 "id": "phase12-cleanup-handler-during-error",
 "setup": [],
 "expr": "(condition-case e (unwind-protect (/ 1 0) (ignore-errors (car 6))) (error e))",
 "note": "Audit probe A1, the shape Phase 11 recorded and carried forward: the cleanup runs while an arith-error is being unwound and handles an error of its own. The in-flight condition survives, because the cleanup's error never becomes a completion. kg answered (wrong-type-argument listp 6) before the pin -- the cleanup's error clobbering the one the handler was waiting for."
}
