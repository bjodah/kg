# Sub-plan 09D Part 2.  The arena-diagnostics command's output, on a real
# terminal.  test/lisp-compat/features.json's `command-lisp-arena-stats`
# row cites this case and test_cmd.c:test_lisp_arena_stats_renders; that
# one pins the rendering in-process, this one pins that the same line
# reaches the echo area of a live editor.
#
# No Emacs oracle, by design: Emacs has no arena, no fixed slot count and
# no collector of fe's shape, so this is kg policy pinned against kg's own
# measured behaviour (the 09 set's compatibility-direction note).
#
# The wide terminal is deliberate, and it got wider: the line is 166
# columns on a session that has only loaded the prelude, because Phase 24
# gave the payload region real numbers to report.  An 80-column screen
# would truncate the `fails` field this case exists to read, and the 120
# it used to ask for now cuts the payload clause in half.
name: lisp-arena-stats-command
filename: lisp-arena-stats.txt
requires_feature: lisp
backend: tmux
dimensions: [24, 200]
initial: ""
keys:
  - M-x
  - lisp-arena-stats
  - RET
  - o
  - k
expected_saved: ok
expected_screen_contains:
  # The counters, in the order the renderer writes them.  440103 slots,
  # a 10909-frame capacity and 2350896 payload bytes are the COMPILED
  # DEFAULT arena's measured partitioning at the string pin
  # (doc/fe-upstream.md's row).
  # That default is 10 MiB since Phase B of
  # doc/plans/2026-08-19-fe-simplification-and-cheap-compat.md; the same fe
  # partitions the 1 MiB arena it replaced into 41971/1078, which is the
  # arena the pin series below was measured against -- 56147/1087 at the
  # let-binding-buffer-tag pin, 56263/1089 at the Phase 20 pin, 56259/1090
  # at the Phase 19 pin, 56225/1095 at the Phase 12 pin, 56226/1095 at the
  # Phase 11 pin and 56224/1096 at Phase 10's.  fe re-partitions at some
  # pins and the host may ask for any size, so both numbers here are
  # measured, never carried forward.
  # The payload substrate took NO bytes at the pin that introduced it,
  # because `FeOpenContext` carved no region; what moved there was
  # `FeMinimumArenaSize()`, by 280 bytes -- 224 for the fourteen objects the
  # `payload-exhaustion` condition row builds at context open, and 56 for
  # the seven `FeContext` fields the region's bookkeeping adds to the
  # arena's fixed header.  Phase 24 is where kg started asking for the
  # region (src/lisp_core.c's lisp_arena_options), because a vector's
  # elements live in it, and a quarter of what is left after the frames
  # went to it: 586993 object slots became 440489 beside 2344064 payload
  # bytes.  Phase 25 made a string's bytes payload too, so there is no
  # longer an uncarved partition to compare against: the region is funded
  # from `FeMinimumArenaSize()` before the percentage divides anything,
  # and the same 10 MiB now reads 440103 / 10909 / 2350896.
  # Phase 26's fe indexes the obarray, and the index's table is payload
  # with one stable owner, so `FeMinimumArenaSize()` grew 72136 -> 74248
  # to fund the first block: the same 10 MiB reads 440190 / 10911 /
  # 2349296 at the Phase 25 pin and 440103 / 10909 / 2350896 here.
  # Phase 14's fe widened a symbol object by one cons and added eight
  # primitives, which grew FeMinimumArenaSize by 2264 bytes; the arena
  # partition answers by moving two frame slots' worth of bytes to the
  # object side, so the object count RISES as the frame count falls.
  # `fails 0` is the claim that a session which has only booted has never
  # failed an allocation.  `GC 1`, not `GC 0`: the post-prelude collect
  # (doc/plans/2026-08-14-embedded-prelude.md, "Post-prelude collect --
  # results") is kg_lisp_init()'s own last act, so a session that has only
  # booted has already collected exactly once by the time this command can
  # run.
  - "Arena: 440103 slots,"
  - " free, peak "
  - "; GC 1; roots "
  - "/10909; fails 0"
  # The payload region, from the same line.  The CAPACITY is pinned here
  # because it is a property of the arena, like the slot count above it,
  # and `payload fails 0` because a booted session having been refused
  # nothing is this case's claim.  What the session has USED of it is not
  # pinned here: since Phase 25 that is every symbol name the prelude
  # interned (35496 bytes live, 66128 peak, one compaction as this case
  # was written), and .ci/prelude-startup-census.json is the ratchet that
  # owns those three -- spelling them twice would only mean two places to
  # correct when the prelude gains a definition.
  - "/2350896 bytes, peak "
  - "; payload fails 0"
expected_screen_not_contains:
  - "Lisp not available"
