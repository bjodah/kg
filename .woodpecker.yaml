variables:
  - &ci_image 'cont-reg.bjodah.se:443/bjodah/triceratops-5:43'

when:
  - event: [push]

steps:
  - name: build-and-test
    image: *ci_image
    environment:
    commands:
      - apt-get install pmccabe
      - cp -ra . /dev/shm && env -C /dev/shm bash -l .ci/run-ci-steps.sh
