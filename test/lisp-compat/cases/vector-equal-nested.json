{
 "id": "vector-equal-nested",
 "setup": [],
 "expr": "(list (equal [[1] [2]] [[1] [2]]) (equal [[1] [2]] [[1] [3]]))",
 "note": "`equal' recurses into vector elements: (t nil). The second half is the half that catches an implementation comparing element identity instead of element structure -- both sides read fresh inner vectors, so an `eq'-per-element `equal' would answer nil for BOTH."
}
